# hello-world

The quintessential "Hello, world!" program, in Python.

## Run

```sh
python3 hello.py
```

Expected output:

```
Hello, world!
```

## CI

Every push and pull request also runs `hello.py` via GitHub Actions — see the
[Actions tab](../../actions) for output.
